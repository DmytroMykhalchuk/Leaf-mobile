import { Text } from "@gluestack-ui/themed";

type ProfileSavedScreenType = {
};

export const ProfileSavedScreen: React.FC<ProfileSavedScreenType> = ({ }) => {

    return (
        <>
            <Text>ProfileSavedScreen</Text>
        </>
    );
};