import { Text } from "@gluestack-ui/themed";

type ProfileCoursesScreenType = {
};

export const ProfileCoursesScreen: React.FC<ProfileCoursesScreenType> = ({ }) => {

    return (
        <>
            <Text>ProfileCoursesScreen</Text>
        </>
    );
};