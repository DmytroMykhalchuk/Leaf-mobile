import { StyleSxProps } from "../../constants/layout";

export const inputStyles = {
    borderColor: '$light400',
    // borderWidth: 1,
    ':focus': {
        borderColor: '$info400',
    }
} as StyleSxProps;